{
  "name": "shibori",
  "version": "1.0.0",
  "private": false,
  "homepage": "http://copyaplexy.github.io/shibori",
  "dependencies": {
    "bootstrap": "^4.2.1",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.2",
    "reactstrap": "^6.5.0"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "clean-gh-cache": "rimraf ./node_modules/.cache/gh-pages",
    "deploy": "gh-pages -d build",
    "clean + deploy": "yarn clean-gh-cache && yarn deploy"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
